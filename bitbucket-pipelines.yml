image: microsoft/dotnet:sdk

pipelines:
  default:
    - step:
        caches:
          - dotnetcore
        script:
          - export PROJECT_NAME=PoE.Bot.csproj
          - export TEST_NAME=PoE.Bot.Tests.csproj
          - dotnet restore
          - dotnet build $PROJECT_NAME